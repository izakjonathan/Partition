# Set both secrets before connecting the database. Each must be at least 32 characters.
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=
# Set after verifying unauthenticated / redirects and /api/export returns 401.
DATABASE_URL=
